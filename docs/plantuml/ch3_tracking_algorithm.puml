```plantuml
@startuml
skinparam handwritten false
skinparam shadowing false
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam nodesep 40
skinparam ranksep 40

start

:BoT-SORT theo vết và cấp ID\ncho phương tiện (v_tid); <<#DAE8FC>>

:Cắt ảnh phương tiện\ntheo Bounding Box;

:YOLOv8-OBB tìm biển số\ntrên vùng ảnh đã cắt; <<#DAE8FC>>

if (Tìm thấy biển số?) then (Có)

  :Đếm số Bounding Box phương tiện\nchứa tâm biển số; <<#FFF2CC>>

  if (Chỉ một phương tiện?) then (Có)
    :Gán biển số cho phương tiện đó\n(v_tid); <<#D5E8D4>>
  else (Nhiều phương tiện)
    :Gán biển số cho phương tiện có\nBounding Box nhỏ nhất; <<#D5E8D4>>
  endif

  :Cắt ảnh biển số\nđể đưa vào OCR;

else (Không)
  :Bỏ qua khung hình này;
endif

stop
@enduml
```
